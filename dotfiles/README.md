# dotfiles moved

The Claude Code config (settings.json, CLAUDE_SELF.md, hook scripts, agents) lives in the standalone repository https://github.com/j0yen/dotfiles, cloned at `~/dotfiles` on every node and kept current by fleet-sync (`~/wintermute/fleet-sync`). Its `install.sh` links the files into `~/.claude`. This directory was folded into that repository on 2026-09-02; do not put files here.
